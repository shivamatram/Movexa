<?xml version="1.0" encoding="utf-8"?>
<!-- Maintenance Task Item — used in mechanic dashboard queue -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingVertical="@dimen/spacing_small"
    android:background="?attr/selectableItemBackground">

    <!-- Urgency indicator bar -->
    <View
        android:id="@+id/viewUrgencyBar"
        android:layout_width="4dp"
        android:layout_height="match_parent"
        android:layout_marginEnd="@dimen/spacing_small"
        android:background="@color/warning" />

    <!-- Content -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <!-- Vehicle number & service type -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tvVehicleNumber"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="@dimen/text_body"
                android:textColor="@color/text_primary"
                android:fontFamily="sans-serif-medium"
                tools:text="MH-04-AB-1234" />

            <TextView
                android:id="@+id/tvUrgencyBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_overline"
                android:textColor="@color/on_error"
                android:paddingHorizontal="@dimen/spacing_small"
                android:paddingVertical="@dimen/spacing_xxs"
                android:fontFamily="sans-serif-medium"
                tools:text="OVERDUE" />

        </LinearLayout>

        <!-- Service type -->
        <TextView
            android:id="@+id/tvServiceType"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_caption"
            android:textColor="@color/text_secondary"
            android:layout_marginTop="@dimen/spacing_xxs"
            tools:text="Oil Change Service" />

        <!-- Due info -->
        <TextView
            android:id="@+id/tvDueInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="@dimen/text_caption"
            android:textColor="@color/text_hint"
            android:layout_marginTop="@dimen/spacing_xxs"
            tools:text="Overdue by 200 km" />

    </LinearLayout>

    <!-- Arrow/chevron -->
    <ImageView
        android:layout_width="@dimen/icon_small"
        android:layout_height="@dimen/icon_small"
        android:src="@drawable/ic_chevron_right"
        app:tint="@color/text_hint"
        android:contentDescription="@null"
        android:layout_marginStart="@dimen/spacing_small" />

</LinearLayout>
